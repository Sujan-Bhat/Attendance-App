# .env.example - copy to .env and edit
PROJECT_NAME=Attendance API
PROJECT_VERSION=0.0.1

# Postgres DB (example)
DATABASE_URL=postgresql+asyncpg://sujan:anagha@localhost:5432/attend

# Redis
REDIS_URL=redis://redis:6379/0

# JWT and QR secrets (change in production)
JWT_SECRET_KEY=replace_with_strong_jwt_secret
HMAC_QR_SECRET=replace_with_strong_hmac_secret

# QR TTL in seconds
QR_TTL_SECONDS=600

